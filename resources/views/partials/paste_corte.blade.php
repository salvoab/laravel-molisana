<section class="paste-lunghe">
    <h2>LE CORTE</h2>
    <div class="container">
        @foreach($paste_corte as $indice => $pasta)
            @include('partials.pasta')
        @endforeach
    </div>
</section>